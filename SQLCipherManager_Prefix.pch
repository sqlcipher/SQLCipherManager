//
// Prefix header for all source files of the 'SQLCipherManager' target in the 'SQLCipherManager' project.
//

#ifdef __OBJC__
    #import <Foundation/Foundation.h>

    #ifndef NDEBUG
        #ifndef DLog
            #define DLog(fmt, ...) NSLog((@"%s [Line %d] " fmt), __PRETTY_FUNCTION__, __LINE__, ##__VA_ARGS__);
        #endif
    #else
        #ifndef DLog
            #define DLog(...) /* */
        #endif
    #endif
#endif
